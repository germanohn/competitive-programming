#include <bits/stdc++.h>
#define ff first
#define ss second
#define pb push_back
#define mp make_pair
#define debug (args...) fprintf (stderr, args)
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

const int MAX = 11234;

int main () {
	scanf ("%d", &c);
	while (c--) {
		scanf ("%d %d %d", &n, &t, &m);
		for (int i = 0; i < m; i++) {
			scanf ("%d %s", &tempo, side);
			if (side == "left") 
				q[0].push (tempo);
			else 
				q[1].push (tempo);
		}
		int mi = 
		int cont = 0;
		while (cont < m) {
			
		}
	}
}
