#include <bits/stdc++.h>
#define ff first
#define ss second 
#define pb push_back
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

const int MAX = 100005;

int n, cont;
int list[MAX], head[MAX], nx[MAX];
bool mark[MAX];
map<pii, int> del;

int main () {
    scanf(" %d", &n);

}

