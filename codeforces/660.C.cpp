#include<bits/stdc++.h>
#define ff first
#define ss second
#define pb push_back
#define mp make_pair
#define debug (args...) fprintf (stderr, args)
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

const int MAX = 300005;

int n, k;
int v[MAX];

int main () {
    scanf ("%d %d", &n, &k);
    for (int i = 0; i < n; i++) 
        scanf ("%d", &v[i]);
    int ini, fim, i = 0, ans = INT_MIN;
    for (int i = 0; i < n; i++) {
        if (q == 
        if (q == k && v[i] == 0) {
        
        } else if (q == k && v[i] == 1) {
            
        }
    }
}

