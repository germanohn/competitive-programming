#include <bits/stdc++.h>
#define ff first
#define ss second 
#define pb push_back
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

const int MAX = 100005;

int n, m;
pii sub[MAX];

int main () {
    scanf(" %d %d", &n, &m);

    for (int i = 0; i < m; i++) {
        scanf(" %d %d", &sub[i].ff, &sub[i].ss);
    }
}

