#include<bits/stdc++.h>
#define ff first
#define ss second
#define pb push_back
#define mp make_pair
#define debug (args...) fprintf (stderr, args)
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

int main () {
    scanf ("%d", &n);
    for (int i = 0; i < n; i++) 
        scanf ("%d", &v[i]);
    bool alt = false;
    int qOne = 0, qZero = 0, ini, cont = 0, mx = -1;
    for (int i = 1; i < n-1; i++) {
        if (v[i] == 1)
            alt = true;
        
    }
}

