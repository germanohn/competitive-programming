#include <bits/stdc++.h>
#define ff first
#define ss second
#define pb push_back
#define mp make_pair
#define debug (args...) fprintf (stderr, args)
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

char a[105], b[105];

int main () {
	scanf (" %s %s", a, b);
	if (strcmp (a, b) == 0) {
		printf ("%s\n", a);
	} else {
		printf ("1\n");
	}
}

