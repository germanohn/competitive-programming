#include <bits/stdc++.h>
#define ff first
#define ss second 
#define pb push_back
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

const int N = 1e5 + 5;

int n, m;
int swi[N];
bool r[N];

int main () {
    scanf(" %d %d", &n, &m);
    for (int i = 0; i < n; i++) 
        scanf(" %d", &r[i]);
    for (int i = 0; i < m; i++) {
        int x;
        scanf(" %d", &x);
        while (x--) {
                
        }
    }
}

