#include <bits/stdc++.h>
#define ff first
#define ss second 
#define pb push_back
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

const int N = 305;

int m, t, r;
int w[N];

int main () {
    scanf(" %d %d %d", &m, &t, &r);
    for (int i = 0; i < m; i++) 
        scanf(" %d", &w[i]);

    if (t < r) {
        printf("-1\n");
    } else {
        for (int i = 0; i < m; i++) {
            
        }
    }
}

