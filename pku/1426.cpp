#include <bits/stdc++.h>

using namespace std;

int n;

int main () {
    v[0] = 1;
    for (int i = 1; i < 100; i++) {
        v[i] = (v[i - 1] * 10) %  
    }
    while (scanf ("%d", &n) && n != 0) {
         v[0] = 1;
        for (int i = 1; i < 100; i++) {
            v[i] = (v[i - 1] * 10) % n;  
        }

        pd (
    }
}
