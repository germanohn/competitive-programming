#include <bits/stdc++.h>
using namespace std;

int grau[100005], k, w, a, b;

int main () {
    while (scanf ("%d %d", &k, &w) && k != 0 && w != 0) {
        for (int i = 0; i < w; i++) {
        
        }
    }
}
